const { createBot } = require('./bot');

// Create a Minecraft bot with default options
const bot = createBot();

// Additional script-specific logic can be added here
console.log('🎮 Minecraft bot script running...');